#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: the address of the string
 * Return: length of string
 */

int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
