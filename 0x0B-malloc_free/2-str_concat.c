#include "main.h"
#include <stdlib.h>
#include <stdio.h>

char *str_concat(char *s1, char *s2)
{
	/*int len1, len2;

	len1 = len2 = 0;

	while (*s1 != 0)
		len1++;
	while (*s2 != 0)
		len2++;*/
	printf("first letter: %c, second letter : %c\n", *s1, *s2);
	/*printf("len1 : %d, len2: %d\n", len1, len2);*/
	return (NULL);
}
