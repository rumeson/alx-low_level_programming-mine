#include "main.h"
#include <stdio.h>

/**
 * print_diagsums - prints the sum of the two diagonals of a
 * square matrix of integers.
 * @a: the array
 * @size: the size of the array
 */

void print_diagsums(int *a, int size)
{
/*	int f_diag;
	int s_diag;
	int t_sum;
*/
printf("int *a : %d,   int size : %d\n", *(a + 3), size);
}
